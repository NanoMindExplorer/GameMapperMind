<?xml version="1.0" encoding="utf-8" ?>
<!--
  REBUILD: AndroidManifest.xml for Android 12+ (minSdk=31) with 3-path injection scheme.

  Architecture:
  - MainActivity: Capacitor WebView host, intercepts gamepad KeyEvents/MotionEvents
  - FloatingOverlayService: transparent overlay for in-game button visualization
  - GamepadListenerService: foreground service that runs Shizuku getevent stream
  - TouchDaemonService: Shizuku user service (NOT declared here — Shizuku manages it)
    Runs as shell uid, performs 3-path touch injection (AIDL → InputManager → shell)
  - ShizukuProvider: allows app to communicate with Shizuku binder
  - InstalledGamesPlugin: lists installed games + launches them via Intent
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="${cleartextEnabled}"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"/>
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Shizuku v3 Support -->
        <meta-data android:name="moe.shizuku.client.V3_SUPPORT" android:value="true" />

        <service
            android:name=".FloatingOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating Gamepad Overlay"
            />
        </service>

        <service
            android:name=".GamepadListenerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Gamepad Input Listener"
            />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
        />
    </application>

    <!-- ============ PERMISSIONS ============ -->

    <!-- Overlay window for in-game button visualization -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Server (dev mode) + Capacitor WebView asset loading -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground services (GamepadListenerService + FloatingOverlayService) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Keep app alive in background (prevents Android from killing the binder) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Bluetooth gamepad support (Android 12+ runtime permissions).
         NOTE: The app itself does NOT use BluetoothAdapter directly — gamepad
         Bluetooth pairing is handled by Android system. These permissions are
         declared so the system routes gamepad input to the app correctly. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- InstalledGamesPlugin: list installed games + launch them -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Shizuku binder permission -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- ============ HARDWARE FEATURES ============ -->

    <uses-feature android:name="android.hardware.gamepad" android:required="false"/>
    <uses-feature android:name="android.hardware.bluetooth" android:required="false"/>
    <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="true"/>

    <!-- ============ QUERIES (Android 11+ package visibility) ============ -->

    <queries>
        <provider android:authorities="moe.shizuku.manager.shizuku.provider" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>
</manifest>

