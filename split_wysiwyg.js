const fs = require('fs');
const content = fs.readFileSync('src/components/OverlayWysiwyg.tsx', 'utf-8');

// I will write 5 components here which encapsulate the logic safely.
// We must guarantee that everything works.
